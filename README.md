# KubeSim React (Animated UI)

Run locally:

```bash
cd react
npm install
npm run dev
```

Open `http://localhost:5173` and try the interactive terminal. Type `help` or use exercises like `deploy-nginx`.
